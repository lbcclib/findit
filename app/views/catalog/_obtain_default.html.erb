<% if online_record? document %>
<%= render partial: 'catalog/obtain_online', locals: {type: document.first('format'), url: document.first('url_fulltext_display')}%>
<% else %>
<%= render partial: 'catalog/obtain_evergreen', locals: {record_id: document.first('eg_tcn_t')} if evergreen_record? document%>
<% end %>

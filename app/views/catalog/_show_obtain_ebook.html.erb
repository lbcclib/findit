<div class="card">
  <div class="card-body">
    <div class="row">
      <%= "#{mdi_svg('check')} #{t('obtain.source', source: document['record_source_facet'].first)}".html_safe %>
    </div><div class="row">
      <%= render partial: "catalog/obtain_ebook", locals: {document: @document} %>
    </div>
  </div>
</div>
